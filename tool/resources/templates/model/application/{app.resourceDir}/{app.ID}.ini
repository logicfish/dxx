[app]
organizationName = "{{desc.app.organizationName}}"
applicationName = "{{desc.app.appName}}"
configFile = "{{desc.app.ID}}.json"
configDefaults = "{{desc.app.ID}}-default.json"
lang = "{{desc.app.ID}}-enGB"
